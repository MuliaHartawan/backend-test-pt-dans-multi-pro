const jobs = require('./jobs');
const job = require('./job');
const create = require('./create');
const update = require('./update');
const destroy = require('./delete');

module.exports = {
    jobs, job, create, update, destroy
}